<section class="ed-section">
  <div class="ed-container ed-align-right">
    <div class="ed-accent-rule ed-accent-rule-right" aria-hidden="true"></div>
    <h2 class="ed-section-label">See it in action</h2>
    <p class="ed-section-desc">
      Spin up worktrees, develop in parallel, merge and clean up.
    </p>
  </div>
  <div class="ed-container ed-showcase" data-no-zoom>
    <div class="ed-window-glow" aria-hidden="true"></div>
    <figure class="terminal-window">
      <div class="terminal-header" aria-hidden="true">
        <div class="window-controls">
          <span class="control red"></span><span class="control yellow"
          ></span><span class="control green"></span>
        </div>
        <div class="window-title">workmux demo</div>
      </div>
      <div class="video-player" data-video-player>
        <video
          src="/demo.mp4"
          poster="/demo-poster.webp"
          controls
          muted
          playsinline
          preload="metadata"
          aria-describedby="demo-description"></video>
        <button
          type="button"
          class="video-player-toggle"
          aria-label="Play video"
        >
          <span class="video-player-toggle-icon" aria-hidden="true"></span>
        </button>
        <span class="sr-only video-player-state" aria-live="polite"></span>
      </div>
      <figcaption id="demo-description" class="sr-only">
        Silent demonstration of workmux creating, switching between, and merging
        parallel worktrees.
      </figcaption>
    </figure>
  </div>
</section>

<style>
  figure {
    margin: 0;
  }

  .video-player {
    position: relative;
    background: black;
  }

  video {
    display: block;
    width: 100%;
    cursor: pointer;
  }

  .video-player-toggle {
    position: absolute;
    inset: 50% auto auto 50%;
    display: grid;
    width: 4.5rem;
    height: 4.5rem;
    padding: 0;
    transform: translate(-50%, -50%);
    place-items: center;
    border: 0;
    border-radius: 50%;
    color: white;
    background: rgb(255 255 255 / 12%);
    box-shadow: none;
    backdrop-filter: blur(4px);
    cursor: pointer;
    transition:
      opacity 250ms ease,
      transform 250ms ease,
      background-color 150ms ease;
  }

  .video-player-toggle:hover,
  .video-player-toggle:focus-visible {
    transform: translate(-50%, -50%) scale(1.06);
    background: var(--ed-button-bg);
  }

  .video-player-toggle:focus-visible {
    outline: 3px solid white;
    outline-offset: 3px;
  }

  .video-player.is-playing .video-player-toggle {
    opacity: 0;
    pointer-events: none;
  }

  .video-player.is-playing .video-player-toggle:focus-visible {
    opacity: 1;
    pointer-events: auto;
  }

  .video-player-toggle-icon::before {
    display: block;
    width: 0;
    height: 0;
    margin-left: 0.2rem;
    border-block: 0.72rem solid transparent;
    border-inline-start: 1.15rem solid currentColor;
    content: '';
  }

  .video-player-toggle[data-playing='true'] .video-player-toggle-icon::before {
    width: 1.1rem;
    height: 1.35rem;
    margin: 0;
    border: 0;
    background: linear-gradient(
      to right,
      currentColor 0 34%,
      transparent 34% 66%,
      currentColor 66% 100%
    );
  }

  figcaption:not(.sr-only) {
    padding: 0.75rem 1rem;
    color: rgb(255 255 255 / 65%);
    background: #252525;
    font-size: 0.8rem;
    text-align: center;
  }

  @media (prefers-reduced-motion: reduce) {
    .video-player-toggle {
      transition: none;
    }
  }
</style>
