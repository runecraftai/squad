# ADR-001: LifeForge interface

The Personal OS proof uses a Hermes skill and a separate LifeForge MCP adapter.

The skill owns conversational guidance while the MCP server owns semantic data operations.

This Squad-side adapter is independent and does not implement LifeForge calls.
