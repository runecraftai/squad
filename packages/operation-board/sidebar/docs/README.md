# workmux documentation

The published site lives at [workmux.raine.dev](https://workmux.raine.dev/).

```bash
bun install
bun run dev
```

Run `bun run check`, `bun run format:check`, and `bun run build` before
submitting documentation changes.
