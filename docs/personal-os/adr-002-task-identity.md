# ADR-002: Squad external identity

The external Squad abstraction is a task.

The stable identifier is named `taskId`.

A session identifier is not exposed because conversation continuity belongs to the client.
