# Decision hold lifecycle

The normative policy is owned by `.agents/skills/decision-hold-lifecycle/SKILL.md`.
The deterministic mechanism (`hold`, `complete`, `resolve`, `verify`) is owned by `bin/sq-decision-hold.sh --help`.
